Find where User is defined and add email validation.
Requirements:
- Update model validation to enforce a basic email format check
- Update related references so views use Customer naming
- Keep changes minimal across files.
