Write a Python function in fibonacci.py named fib that computes the nth Fibonacci number.
Requirements:
- Signature: def fib(n: int) -> int
- Handle n=0 and n=1 correctly
- Raise ValueError for negative n
- Keep implementation simple and readable.
